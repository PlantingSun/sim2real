# Step 4.6：独立 policy 进程测试

本阶段只验证 policy 放入独立进程后的 CPU 调度和延时，不初始化 DDS，也不接触机器人。

```bash
python scripts/policy/benchmark_policy_process.py \
  --cpus 2,3 --threads 1 --warmup 50 --iterations 500
```

先比较 `--threads 1` 和 `--threads 2`。该结果用于确认独立进程本身不会降低 policy
性能；通过后再进入 4.7。
